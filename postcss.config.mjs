// Tailwind CSS v4 — the approved UI stack (STEP 02 §2). Next.js picks this up
// automatically; there is no separate Tailwind config file in v4, the theme
// lives in CSS (`app/globals.css`).
const config = {
  plugins: {
    '@tailwindcss/postcss': {},
  },
};

export default config;
